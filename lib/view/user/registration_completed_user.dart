
import 'package:flutter/material.dart';

class RegistrationCompleted extends StatelessWidget {
  
  const RegistrationCompleted({Key? key, required}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(''),
      ),
    );
}
}